<core:FragmentDefinition xmlns="sap.m" xmlns:core="sap.ui.core">
	<Popover showHeader="false" placement="Right" offsetX="{/positionX}" offsetY="{/positionY}" afterClose="contextMenuClosed">
		<List items="{/commands}">
			<ActionListItem text="{description}" type="{path:'deactivate', formatter: '.deactivateToListType'}" class="ppmFlowCommand"/>
		</List>
	</Popover>
</core:FragmentDefinition>